# Tasks — 129 tribe themes

**Status:** Draft. Gates per task: fmt, clippy -D warnings, cargo test --workspace.

- [ ] **T1 — Theme wiring.** /me exposes the selected world's tribe; base.html probe sets/clears
  data-theme on the root (path-gated to /w/), localStorage pre-paint snippet; integration tests
  (AC1). (AC1)
- [ ] **T2 — The three skins + signatures.** The full token/component blocks per the plan's
  design system (palettes, radius, headers with ornament SVGs, tables, nav, status strip) and
  the three signature buttons (cape swing / forge ember / leaf unfurl), reduced-motion gated;
  static CSS pins (AC2/AC3/AC4/AC6).
- [ ] **T3 — Visual acceptance + review.** Operator/self screenshots per tribe on live pages;
  fix taste findings; full suite green (AC5); reviewer APPROVE; statuses flipped; merged when
  Verified.

## Done when

All ACs pass with tests, screenshots reviewed, gates green, reviewer APPROVE, merged once
Verified.
