# Tasks — 108 global countdown

Branch `feature/108-global-countdown`.

- [x] **T1**: add the canonical `.countdown` ticker (tick + reload-on-complete, TDZ-safe) to base.html.
- [x] **T2**: remove the per-page copies (detail/village/academy/smithy/troops) — base.html covers them.
- [x] **T3 — Verify**: live — the detail-page countdown ticks via the global ticker; the Marketplace + Rally
  Point pages now carry it. Tests assert the market/rally pages contain the global ticker.
- [ ] **T4 — Reviewer + PR.**
