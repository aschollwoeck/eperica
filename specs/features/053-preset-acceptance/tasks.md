# Tasks — 053 preset acceptance + ADR finalization

- [x] **T1** — Acceptance test `classic_and_speed_worlds_are_served_divergent_rules` (registry serves classic
  vs speed divergent bundles at the same GameSpeed; home stays classic).
- [x] **T2** — ADR 0035 → Accepted; CLAUDE.md notes the program (047–053) complete (roadmap.md doesn't track
  this program — the ADR does). Gate + reviewer.

Gates: `fmt --check`, `clippy -D warnings`, `cargo test --workspace`, P11.
