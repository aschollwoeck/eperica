# Tasks — 112 slot-building polish

- [ ] **T1** — domain: `is_demolishable` excludes the Palace; test. (AC1)
- [ ] **T2** — web: `_upgrade.html` — an unbuilt **building** shows a "Build on a free slot" link to the
  plan (not a fixed-slot POST); fields/upgrades unchanged. Update the affected test. (AC2)
- [ ] **T3** — CSS: tidy the 22-slot plan (reserved emphasis, clearer empty spots). (AC3)
- [ ] **T4** — reviewer + PR + merge + restart.
