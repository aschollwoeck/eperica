# Tasks — 082 styleguide refresh

Gated by fmt/clippy/`cargo test --workspace`/P11. Presentation only; branch `feature/082-styleguide-refresh`.

- [x] **T1 — Rewrite styleguide.html** onto theme-steel + the design-system components (phead, statgrid,
  bld-card, ribbon, roster unit, tabs, repcard, conversations/messages, alerts, form/table, wonder-board);
  keep the favicon link + standalone <head>.
- [ ] **T2 — Gate + reviewer + PR.** fmt/clippy/`cargo test --workspace` green; reviewer APPROVE; PR opened.
