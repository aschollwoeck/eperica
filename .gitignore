# Rust
/target
**/target
**/*.rs.bk
# Cargo.lock IS committed (this is an application, not a library)

# Environment / secrets
.env
.env.*
!.env.example

# Tooling / local
.claude/settings.local.json
*.log
debug.log

# Stray .NET build artifacts from the deleted legacy project (defensive)
**/obj/
**/bin/
*.cache

# OS / editor
.DS_Store
Thumbs.db
.vs/
.idea/
*.swp
