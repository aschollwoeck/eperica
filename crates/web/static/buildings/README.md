# Building background images

Drop one WebP per building here, named `<slug>.webp` (e.g. `smithy.webp`, `main_building.webp`).
Prompts + slug list: `docs/art/building-backgrounds.md`. Missing files fall back gracefully (no image, just the dark theme).
