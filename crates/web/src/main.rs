//! Eperica web — the server entrypoint (HTTP/UI), wiring the application and infrastructure layers.
//!
//! Slice 001 / T1 establishes the workspace only; the HTTP server, configuration, and persistence
//! are added in later tasks (T2–T16).
#![forbid(unsafe_code)]

fn main() {
    println!("Eperica web — workspace scaffolding (slice 001, T1).");
}
